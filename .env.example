# ====================================================================
# 全栈环境变量配置参考模板 (Environment Variables Template)
# 复制本文件为 .env 即可在本地或生产环境覆盖默认配置
# ====================================================================

# --------------------------------------------------------------------
# 1. 数据库持久层配置 (MySQL 8.0)
# --------------------------------------------------------------------
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=go_zero_boilerplate
MYSQL_DSN=root:root@tcp(127.0.0.1:3306)/go_zero_boilerplate?charset=utf8mb4&parseTime=true&loc=Asia%2FShanghai

# --------------------------------------------------------------------
# 2. 缓存集群配置 (Redis 7)
# --------------------------------------------------------------------
REDIS_HOST=127.0.0.1:6379

# --------------------------------------------------------------------
# 3. 服务注册与发现中心 (Nacos / Etcd)
# --------------------------------------------------------------------
# 模式一：Nacos 服务注册发现（默认）
NACOS_HOST=127.0.0.1
NACOS_PORT=8848
NACOS_NAMESPACE=public

# 模式二：Etcd 服务注册发现（备选）
ETCD_HOST=127.0.0.1:2379

# --------------------------------------------------------------------
# 4. 微服务网络监听与网关寻址 (gRPC Services)
# --------------------------------------------------------------------
USER_RPC_LISTEN_ON=0.0.0.0:8080
USER_RPC_HOST=127.0.0.1:8080

ORDER_RPC_LISTEN_ON=0.0.0.0:8081
ORDER_RPC_HOST=127.0.0.1:8081

# --------------------------------------------------------------------
# 5. 统一对外 HTTP 网关 / BFF (Gateway)
# --------------------------------------------------------------------
GATEWAY_HOST=0.0.0.0
GATEWAY_PORT=8888
JWT_SECRET=go-zero-boilerplate-secret-key-2026
JWT_EXPIRE=86400

# --------------------------------------------------------------------
# 6. 前端多端端口 (Frontend Vite Apps)
# --------------------------------------------------------------------
PORTAL_PORT=3000
ADMIN_PORT=3001
VITE_API_URL=http://localhost:8888
