## 1. 架构准备与依赖集成

- [x] 1.1 在前端 `apps/admin` 中引入 `bpmn-js` 与相关类型支持，并在 `vite.config.ts` 中配置 `vendor-bpmn` 异步分包，验证 `pnpm --filter admin build` 构建无警告。
- [x] 1.2 执行 `just new-rpc itsm` 初始化后端 ITSM 微服务工程骨架与基础配置，验证 `go build ./app/itsm/rpc/...` 编译通过。
- [x] 1.3 编写 ITSM 核心数据表结构 DDL（流程定义表、流程实例表、任务流转表、工单动态数据表、流转日志表、SLA 策略表），执行数据库迁移并验证数据表与索引创建成功。

## 2. 后端流程引擎与接口契约实现

- [x] 2.1 实现 BPMN 2.0 XML 基础解析与拓扑校验模块，编写单元测试验证节点提取（UserTask/Gateway）与连线流转有效性。
- [x] 2.2 实现工单状态机与流转 Logic（创建工单、任务认领、审批通过、驳回、加签、会签判定），并通过自动化测试覆盖分支流转逻辑。
- [x] 2.3 结合 `app/worker` 的 Temporal 定时器实现 SLA 倒计时与超时升级异步活动，验证超时触发状态流转与站内信通知派发。
- [x] 2.4 在 `app/gateway/desc/itsm.api` 编写 RESTful 契约，执行 `just gen-gateway` 与 `just gen-ts` 生成网关桩代码及前端强类型 SDK。

## 3. 前端 BPMN 设计器与动态表单引擎

- [x] 3.1 封装基于 `bpmn-js/lib/Modeler` 的 React 画布组件与快捷操作栏（放大、缩小、居中、导出 XML/SVG），验证画布基础交互与暗黑模式适配。
- [x] 3.2 基于 Ant Design 6.x 与 ProComponents 构建自定义节点属性配置抽屉（审批人/角色绑定、会签规则、节点字段权限矩阵），验证属性变更与 BPMN Moddle 扩展元素的双向绑定。
- [x] 3.3 基于 `@ant-design/pro-components` 的 `BetaSchemaForm` 封装动态表单渲染组件，验证根据流程节点权限配置动态切换字段只读、必填、隐藏与编辑状态。
- [x] 3.4 基于 `bpmn-js/lib/NavigatedViewer` 实现流程流转轨迹查看器，实现已处理（绿色）、待办理（蓝色脉冲）节点高亮着色及悬浮信息卡片。

## 4. 前端工单工作台与业务闭环

- [x] 4.1 基于 `ProTable` + `PageContainer` 打造 ITSM 工单中心视图（我的待办、我的已办、我发起的、部门工单、预警工单），验证状态标签、优先级徽标及过滤搜索。
- [x] 4.2 开发工单详情综合页（左侧动态表单/时间轴，右侧 BPMN 可视化轨迹图，底部快速审批/退回/转派抽屉），验证完整工单处理闭环。
- [x] 4.3 开发服务目录配置与流程发布管理界面，支持流程定义导入、设计、表单绑定与一键部署发布。

## 5. 全链路验证与交付

- [x] 5.1 运行全栈测试套件与静态规范检查，确保 `just test-frontend`、`just lint-antd`、`go build ./...` 均通过。
- [x] 5.2 模拟跨部门审批场景进行端到端流转验证，确认状态一致性、SLA 倒计时及日志审计完整。
- [x] 5.3 整理 ITSM 架构设计方案与操作指引，输出设计文档并归档。
